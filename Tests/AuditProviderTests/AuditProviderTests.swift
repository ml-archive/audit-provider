import XCTest

@testable import AuditProvider

class AuditProviderTests: XCTestCase {
    static var allTests = [
        ("testBasic", testBasic)
    ]

    func testBasic() {
        XCTAssert(true)
    }
}